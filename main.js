// import app.js
const app = require('./app'); 

const PORT = 3000;

// TODO- delete 
// start the server
app.listen(PORT, () => {
    console.log(`=============================================`);
    console.log(`🚀 WebServer is running on http://localhost:${PORT}`);
    console.log(`=============================================`);
});

// TODO- add
// start the server
//app.listen(PORT);