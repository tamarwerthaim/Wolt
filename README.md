# Wolt

## How to Run Tests
To build and run the unit tests within the Docker container:
1. Create a build directory: `mkdir -p build && cd build`
2. Configure CMake: `cmake ..`
3. Build the tests: `make unit_tests`
4. Run: `./unit_tests`